💡 DSM performance bottleneck is refinement, not the O(n²) DP partitioner. Profiling showed `optimal-partition` was relatively cheap, while `partition-cost` dominated because node/block refinement re-ran full order→interval-costs→DP evaluation for many candidate swaps. Safe local proxy gates helped: pruning node/block swaps by non-improving local edge-length delta reduced runtime without changing acceptance semantics. Also learned two scoring pitfalls: crossing-count alone degenerates to one giant block, and additive linear `β * block_size` is constant over a full partition, so it cannot affect partition choice. Quadratic size penalty works, but needs tuning; current default `β=0.05` is only a compromise between anti-blob pressure and over-fragmentation.