# Munera Plan

Open tasks in intended order:

- `011-make-local-branch-locality-explicit-and-tighten-semantic-ownership` — make branch locality explicit in local step evidence, migrate downstream consumers off proxy inference, and tighten the remaining semantic ownership boundaries in `gordian.local.*`
