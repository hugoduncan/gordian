# Munera Plan

Open tasks in intended order:

- `001-local-comprehension-complexity` — define and implement a new local-analysis metric lens for code in the small
- `002-cyclomatic-complexity` — mostly implemented; finish remaining checklist items and convergence cleanup (legacy docs/schema cleanup, final text/README consistency, any remaining compatibility decisions)
