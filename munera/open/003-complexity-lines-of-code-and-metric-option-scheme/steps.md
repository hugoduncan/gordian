- [ ] Write the implementation companion doc first, locking CLI examples, schema examples, LOC rules, sort semantics, threshold semantics, display-only filter semantics, and explicit `--min-cc` removal
- [ ] Refactor pure complexity option parsing/finalization so generic `--min metric=value` handling and two-metric sort/truncation semantics live in pure code
- [ ] Remove `--min-cc` from CLI parsing, help output, docs, and tests
- [ ] Add clear validation/error messaging that points users from `--min-cc` to `--min cc=...`
- [ ] Implement LOC counting over the whole reported unit form span using non-blank, non-comment physical source-line counting
- [ ] Add canonical unit field `:loc` and rollup fields `:total-loc`, `:avg-loc`, and `:max-loc`
- [ ] Add explicit report metadata for the fixed two-metric payload, including `:metrics [:cyclomatic-complexity :lines-of-code]`
- [ ] Implement allowed `--sort` keys: `cc`, `loc`, `cc-risk`, `ns`, `var`
- [ ] Implement the documented tie-break rules for each sort key
- [ ] Keep `--top` metric-agnostic and apply it after sort, independently per applicable section
- [ ] Implement `--min` as a display filter for unit rows only, leaving namespace and project rollups based on the full analyzed unit set
- [ ] Update human-readable complexity output to always show both `cc` and `loc` in the same unit and rollup tables
- [ ] Implement bar-selection behavior: use the primary ranking metric for `cc`/`loc` sorts, otherwise default bars to `cc`
- [ ] Make display-only filter behavior explicit in help text, docs, and output wording where needed
- [ ] Update EDN/JSON output and schema/docs to reflect the two-metric canonical payload
- [ ] Add tests for LOC counting rules, malformed/unknown `--min` expressions, combined thresholds, `loc` sorting, tie-breaks, bar-selection behavior, display-only filtering of units, unfiltered rollups, two-metric output shaping, and explicit rejection of `--min-cc`
